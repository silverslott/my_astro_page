---
import { Picture } from '@astrojs/image/components';
import Sofia from '~/assets/images/avatar.png';  

interface CallToAction {
  text: string;
  href: string;
  icon?: string;
}

export interface Props {
  title?: string;
  subtitle?: string;
  callToAction?: string | CallToAction;
  callToAction2?: string | CallToAction;
  image?: string | any;
}

const {
  title = await Astro.slots.render('title'),
  subtitle = await Astro.slots.render('subtitle'),
  image = await Astro.slots.render('image'),
} = Astro.props;

---

<section class="relative md:-mt-[76px]">
  <div class="absolute inset-0 pointer-events-none" aria-hidden="true"></div>
  <div class="relative max-w-5xl mx-auto px-4 sm:px-6">
    <div class="pt-0 md:pt-[76px] pointer-events-none"></div>
    <div class="grid grid-cols-1 max-sm:grid-cols-1 py-5 md:pt-[50px] md:pb-[50px]">
      <div class="lg:col-span-2 md:col-span-2 max-sm:col-span-2 text-center max-w-screen-lg mx-auto lg:mt-0 md:mt-[20px]">
        <div class="pb-5 md:pb-10">
        {
          image && (
            <div class="relative m-auto max-w-5xl block">
              {typeof image === 'string' ? (
                <Fragment set:html={image} />
              ) : (
                <Picture
                  class="mx-auto rounded-full"
                  src={Sofia}
                  widths={[400, 768, 1024, 2040]}
                  sizes="(max-width: 767px) 400px, (max-width: 1023px) 768px, (max-width: 2039px) 1024px, 2040px"
                  aspectRatio={200 / 200}
                  loading="eager"
                  width={200}
                  height={200}
                  alt="Sofia_Silverslott"
                  {...image}
                />
              )}
            </div>
          )
        }
      </div>
      <div class="text-2xl max-sm:mt-3">Welcome! I'm</div>
      <div class="text-center pt-2 max-w-screen-lg mx-auto">
        {
          title && (
            <h1
              class="text-6xl md:text-6xl max-sm:text-3xl h-[4rem] md:h-[4.5rem] lg:h-[4.5rem] max-sm:h-[2.5rem]
              font-bold font-bruno inline-block sofia-title"
              set:html={title}
            />
          )
        }
        <div class="max-w-3xl mx-auto lg:h-[70px] md:h-[70px] max-sm:h-[40px]">
          {subtitle && <h2 class="text-xl max-sm:text-xs mb-3 md:text-xl text-yellow-950 font-medium font-code" set:html={subtitle} />}
        </div>
      </div>
      <div id="ab-test-container" class="mt-5">
        <button data-variant="A">Variant A</button>
        <button data-variant="B">Variant B</button>
        <button data-variant="C">Variant C</button>
      </div>      
    </div>
    <div class="lg:columns-2 md:columns-2 lg:col-span-1 md:col-span-2 max-sm:col-span-1 lg:mx-[20px] lg:mt-[40px] lg:mb-[40px] md:mx-[40px] max-sm:mx-[20px] lg:text-xl 
      md:mt-[40px] max-sm:my-5 max-sm:text-center">
      I'm thrilled to share my journey from the academic world to the vibrant realm of <span class="underline decoration-indigo-200 font-bold">iGaming</span>. 
      My path has been anything but 
      conventional, moving from the structured environment of academia to the dynamic and exhilarating world of gaming.<br>
      Transitioning from a <span class="underline decoration-indigo-200 font-bold">Bioinformatician</span> into a 
      <span class="underline decoration-indigo-200 font-bold">CRO Analyst</span>, I discovered that many of the skills I honed in bioinformatics 
      seamlessly translated into the realm of digital marketing. Both disciplines require a strong foundation in 
      <span class="underline decoration-indigo-200 font-bold">data analysis</span> and a keen eye for patterns. Whether I'm decoding the human 
      genome or user behavior on a website, the core principles remain the same: gather data, analyze it meticulously, and derive 
      actionable insights to drive meaningful outcomes.
      <br>My experience in bioinformatics has endowed me with a unique perspective on 
      <span class="underline decoration-indigo-200 font-bold">problem-solving</span>. I approach CRO challenges with a scientific mindset. 
      My analytical skills enable me to dissect user data with precision, while my background in Life Science fuels my 
      commitment to evidence-based strategies.
      Moreover, my versatile role as a Bioinformatician and System Developer at NGI has equipped me with a valuable skill set. 
      I am adept at <span class="underline decoration-indigo-200 font-bold">web development</span> and <span class="underline decoration-indigo-200 font-bold">
      coding</span>, <span class="underline decoration-indigo-200 font-bold">UX</span>, <span class="underline decoration-indigo-200 font-bold">data analysis</span> 
      and <span class="underline decoration-indigo-200 font-bold">visualization</span>- all of which are needed in the realm of CRO. 
      My technical proficiency, coupled with a deep understanding of user behavior, allows me to craft data-driven strategies that 
      drive tangible results.
    </div>
  </div>
</section>

<script client:load>
  const ABTest = {
    testId: 'header_color_test',
    variants: {
      A: '#FD7CDF',
      B: '#FFD048',
      C: '#838EF8'
    },
    originalColor: ''
  };

  const header = document.querySelector('.sofia-title');
  if (header) {
    ABTest.originalColor = header.style.color || getComputedStyle(header).color;
  }

  function setVariant(variant) {
    const savedVariant = localStorage.getItem(`ab_test_${ABTest.testId}`);

    if (savedVariant === variant) {
      localStorage.removeItem(`ab_test_${ABTest.testId}`);
      applyVariant(null);
    } else {
      localStorage.setItem(`ab_test_${ABTest.testId}`, variant);
      applyVariant(variant);
    }
  }

  function applyVariant(variant) {
    if (header) {
      if (!variant) {
        header.style.color = ABTest.originalColor;
      } else {
        header.style.color = ABTest.variants[variant];
      }
    }
  }

  const savedVariant = localStorage.getItem(`ab_test_${ABTest.testId}`);
  if (savedVariant) applyVariant(savedVariant);

  document.querySelectorAll('#ab-test-container button').forEach(button => {
    button.addEventListener('click', () => {
      const variant = button.dataset.variant;
      setVariant(variant);
    });
  });
</script>


<style>
  #ab-test-container button {
    background-color: #f0f0f0;
    border: 2px solid #333;
    color: #333;
    padding: 0.5rem 1rem;
    margin: 0.25rem;
    border-radius: 8px;
    font-weight: bold;
    cursor: pointer;
    transition: background-color 0.2s, transform 0.1s;
  }

  #ab-test-container button:hover {
    background-color: #ddd;
    transform: scale(1.05);
  }

  #ab-test-container button:active {
    transform: scale(0.95);
  }

  #ab-test-container button[data-variant="A"] {
    background-color: #FD7CDF;
    color: black;
    border-color: #8c457b;
  }

  #ab-test-container button[data-variant="B"] {
    background-color: #FFD048;
    color: black;
    border-color: #9a7e29;
  }

  #ab-test-container button[data-variant="C"] {
    background-color: #838EF8;
    color: black;
    border-color: #4b5294;
  }
</style>
